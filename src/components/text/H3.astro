---
interface Props {
	className?: string
}

const { className = "" } = Astro.props
---

<div class={`flex items-center gap-4 ${className}`}>
	<h3 class="text-xl font-bold whitespace-nowrap text-white md:text-3xl">
		<span class="text-accent">01.</span>
		<slot />
	</h3>
	<div class="bg-muted h-px w-1/3"></div>
</div>
