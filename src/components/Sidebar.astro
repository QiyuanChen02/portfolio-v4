---
import { socials } from "../helpers/socials.astro"
import NewTabLink from "./NewTabLink.astro"
---

<aside
	class="fixed bottom-0 hidden w-24 flex-col items-center space-y-4 px-8 lg:flex"
>
	{
		socials.map((social) => {
			const Icon = social.icon
			return (
				<NewTabLink
					href={social.href}
					ariaLabel={social.label}
					className="group h-10 w-10 p-1"
				>
					<Icon class="text-muted group-hover:text-accent h-8 w-8 transition duration-300 group-hover:-translate-y-1" />
				</NewTabLink>
			)
		})
	}
	<div class="bg-muted mt-2 h-30 w-px"></div>
</aside>
